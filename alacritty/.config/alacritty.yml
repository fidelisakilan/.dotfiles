font:
  size: 16.0
  normal:
    family: FiraCode Nerd Font Mono

window:
  opacity: 0.9
  decorations: "Buttonless"

live_config_reload: true 

shell:
  program: /bin/zsh
  args:
    - -l
    - -c
    - "tmux attach || tmux"
